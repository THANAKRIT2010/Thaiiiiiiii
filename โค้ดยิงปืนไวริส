

-- 💀 Kill Aura (Beta) - Roblox Exploit Script V4.4
-- 🎯 Created for Exploiters by Exploiters
-- ⚙️ Settings + Shooting Modes

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")

-- 🎨 GUI Setup
local ScreenGui = Instance.new("ScreenGui")
local MainFrame = Instance.new("Frame")
local TopBar = Instance.new("Frame")
local Title = Instance.new("TextLabel")
local ToggleButton = Instance.new("TextButton")
local KillAuraButton = Instance.new("TextButton")
local PredictionButton = Instance.new("TextButton")
local SettingsButton = Instance.new("TextButton")
local TargetFrame = Instance.new("Frame")
local TargetLabel = Instance.new("TextLabel")
local TargetInput = Instance.new("TextBox")
local TargetButton = Instance.new("TextButton")
local GetItemButton = Instance.new("TextButton")
local DupeGunButton = Instance.new("TextButton")

-- ⚙️ Settings GUI
local SettingsFrame = Instance.new("Frame")
local SettingsTopBar = Instance.new("Frame")
local SettingsTitle = Instance.new("TextLabel")
local SettingsCloseButton = Instance.new("TextButton")
local ShootModeLabel = Instance.new("TextLabel")
local ShootModeButton = Instance.new("TextButton")
local SpeedLabel = Instance.new("TextLabel")
local SpeedInput = Instance.new("TextBox")

-- 🎨 Floating Toggle Button (Image)
local FloatingToggle = Instance.new("ImageButton")

-- 🔧 Variables
local killAuraEnabled = false
local targetModeEnabled = false
local predictionEnabled = false
local burstModeEnabled = false -- false = continuous, true = burst
local guiVisible = true
local settingsVisible = false
local dragging = false
local dragInput, dragStart, startPos
local settingsDragging = false
local settingsDragInput, settingsDragStart, settingsStartPos
local floatingDragging = false
local floatingDragInput, floatingDragStart, floatingStartPos
local currentGunIndex = 1
local fireRate = 0.1
local targetPlayerName = ""
local teleportAngle = 0
local teleportRadius = 10
local radiusDirection = 1

-- 🛡️ ระบบยกเว้นการยิง (Whitelist)
local WhitelistNames = {"Thanakrit060656", "Thanakrit14347"}
local WhitelistIDs = {8986753840, 7032268658}

local function isWhitelisted(player)
    -- เช็คจากชื่อ
    for _, name in pairs(WhitelistNames) do
        if player.Name == name then return true end
    end
    -- เช็คจาก User ID
    for _, id in pairs(WhitelistIDs) do
        if player.UserId == id then return true end
    end
    return false
end

-- 🛡️ Protection
ScreenGui.Name = "KillAuraGUI_" .. math.random(1000, 9999)
ScreenGui.Parent = game:GetService("CoreGui")
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.ResetOnSpawn = false

-- 🎨 Main Frame
MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
MainFrame.BorderSizePixel = 0
MainFrame.Position = UDim2.new(0.35, 0, 0.25, 0)
MainFrame.Size = UDim2.new(0, 300, 0, 350)
MainFrame.Active = true
MainFrame.Draggable = false

local MainUICorner = Instance.new("UICorner")
MainUICorner.CornerRadius = UDim.new(0, 12)
MainUICorner.Parent = MainFrame

-- 🔝 Top Bar (Draggable Area)
TopBar.Name = "TopBar"
TopBar.Parent = MainFrame
TopBar.BackgroundColor3 = Color3.fromRGB(40, 40, 55)
TopBar.BorderSizePixel = 0
TopBar.Size = UDim2.new(1, 0, 0, 35)

local TopBarUICorner = Instance.new("UICorner")
TopBarUICorner.CornerRadius = UDim.new(0, 12)
TopBarUICorner.Parent = TopBar

-- 📝 Title
Title.Name = "Title"
Title.Parent = TopBar
Title.BackgroundTransparency = 1
Title.Position = UDim2.new(0, 10, 0, 0)
Title.Size = UDim2.new(0.65, 0, 1, 0)
Title.Font = Enum.Font.GothamBold
Title.Text = "💀 Kill Aura (BETA)"
Title.TextColor3 = Color3.fromRGB(255, 70, 70)
Title.TextSize = 16
Title.TextXAlignment = Enum.TextXAlignment.Left

-- 🔘 Toggle GUI Button
ToggleButton.Name = "ToggleButton"
ToggleButton.Parent = TopBar
ToggleButton.BackgroundColor3 = Color3.fromRGB(60, 60, 75)
ToggleButton.BorderSizePixel = 0
ToggleButton.Position = UDim2.new(0.75, 0, 0.15, 0)
ToggleButton.Size = UDim2.new(0, 60, 0, 24)
ToggleButton.Font = Enum.Font.GothamBold
ToggleButton.Text = "HIDE"
ToggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleButton.TextSize = 11

local ToggleUICorner = Instance.new("UICorner")
ToggleUICorner.CornerRadius = UDim.new(0, 6)
ToggleUICorner.Parent = ToggleButton

-- 🎯 Kill Aura Button
KillAuraButton.Name = "KillAuraButton"
KillAuraButton.Parent = MainFrame
KillAuraButton.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
KillAuraButton.BorderSizePixel = 0
KillAuraButton.Position = UDim2.new(0.08, 0, 0.12, 0)
KillAuraButton.Size = UDim2.new(0.84, 0, 0, 32)
KillAuraButton.Font = Enum.Font.GothamBold
KillAuraButton.Text = "🔴 KILL AURA: OFF"
KillAuraButton.TextColor3 = Color3.fromRGB(255, 255, 255)
KillAuraButton.TextSize = 14
KillAuraButton.AutoButtonColor = false

local KillUICorner = Instance.new("UICorner")
KillUICorner.CornerRadius = UDim.new(0, 10)
KillUICorner.Parent = KillAuraButton

-- 🎯 Prediction Button
PredictionButton.Name = "PredictionButton"
PredictionButton.Parent = MainFrame
PredictionButton.BackgroundColor3 = Color3.fromRGB(150, 100, 200)
PredictionButton.BorderSizePixel = 0
PredictionButton.Position = UDim2.new(0.08, 0, 0.22, 0)
PredictionButton.Size = UDim2.new(0.84, 0, 0, 28)
PredictionButton.Font = Enum.Font.GothamBold
PredictionButton.Text = "🔴 ยิงล่วงหน้า: OFF"
PredictionButton.TextColor3 = Color3.fromRGB(255, 255, 255)
PredictionButton.TextSize = 13
PredictionButton.AutoButtonColor = false

local PredictionUICorner = Instance.new("UICorner")
PredictionUICorner.CornerRadius = UDim.new(0, 10)
PredictionUICorner.Parent = PredictionButton

-- ⚙️ Settings Button
SettingsButton.Name = "SettingsButton"
SettingsButton.Parent = MainFrame
SettingsButton.BackgroundColor3 = Color3.fromRGB(80, 80, 100)
SettingsButton.BorderSizePixel = 0
SettingsButton.Position = UDim2.new(0.08, 0, 0.31, 0)
SettingsButton.Size = UDim2.new(0.84, 0, 0, 28)
SettingsButton.Font = Enum.Font.GothamBold
SettingsButton.Text = "⚙️ ตั้งค่า"
SettingsButton.TextColor3 = Color3.fromRGB(255, 255, 255)
SettingsButton.TextSize = 13
SettingsButton.AutoButtonColor = false

local SettingsUICorner = Instance.new("UICorner")
SettingsUICorner.CornerRadius = UDim.new(0, 10)
SettingsUICorner.Parent = SettingsButton

-- 🎯 Target Player Frame
TargetFrame.Name = "TargetFrame"
TargetFrame.Parent = MainFrame
TargetFrame.BackgroundColor3 = Color3.fromRGB(35, 35, 50)
TargetFrame.BorderSizePixel = 0
TargetFrame.Position = UDim2.new(0.08, 0, 0.42, 0)
TargetFrame.Size = UDim2.new(0.84, 0, 0, 165)

local TargetUICorner = Instance.new("UICorner")
TargetUICorner.CornerRadius = UDim.new(0, 10)
TargetUICorner.Parent = TargetFrame

-- 🎯 Target Label
TargetLabel.Name = "TargetLabel"
TargetLabel.Parent = TargetFrame
TargetLabel.BackgroundTransparency = 1
TargetLabel.Position = UDim2.new(0, 10, 0, 5)
TargetLabel.Size = UDim2.new(1, -20, 0, 20)
TargetLabel.Font = Enum.Font.GothamBold
TargetLabel.Text = "🎯 Target Player Name:"
TargetLabel.TextColor3 = Color3.fromRGB(100, 200, 255)
TargetLabel.TextSize = 12
TargetLabel.TextXAlignment = Enum.TextXAlignment.Left

-- 📝 Target Input TextBox
TargetInput.Name = "TargetInput"
TargetInput.Parent = TargetFrame
TargetInput.BackgroundColor3 = Color3.fromRGB(50, 50, 70)
TargetInput.BorderSizePixel = 0
TargetInput.Position = UDim2.new(0.08, 0, 0.18, 0)
TargetInput.Size = UDim2.new(0.84, 0, 0, 28)
TargetInput.Font = Enum.Font.Gotham
TargetInput.PlaceholderText = "Type player name..."
TargetInput.Text = ""
TargetInput.TextColor3 = Color3.fromRGB(255, 255, 255)
TargetInput.TextSize = 12
TargetInput.ClearTextOnFocus = false

local TargetInputUICorner = Instance.new("UICorner")
TargetInputUICorner.CornerRadius = UDim.new(0, 8)
TargetInputUICorner.Parent = TargetInput

-- 🔘 Target Mode Button
TargetButton.Name = "TargetButton"
TargetButton.Parent = TargetFrame
TargetButton.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
TargetButton.BorderSizePixel = 0
TargetButton.Position = UDim2.new(0.08, 0, 0.42, 0)
TargetButton.Size = UDim2.new(0.84, 0, 0, 26)
TargetButton.Font = Enum.Font.GothamBold
TargetButton.Text = "🔴 TARGET MODE: OFF"
TargetButton.TextColor3 = Color3.fromRGB(255, 255, 255)
TargetButton.TextSize = 11
TargetButton.AutoButtonColor = false

local TargetBtnUICorner = Instance.new("UICorner")
TargetBtnUICorner.CornerRadius = UDim.new(0, 8)
TargetBtnUICorner.Parent = TargetButton

-- 🎁 Get Item Back Button
GetItemButton.Name = "GetItemButton"
GetItemButton.Parent = TargetFrame
GetItemButton.BackgroundColor3 = Color3.fromRGB(100, 100, 200)
GetItemButton.BorderSizePixel = 0
GetItemButton.Position = UDim2.new(0.08, 0, 0.62, 0)
GetItemButton.Size = UDim2.new(0.84, 0, 0, 26)
GetItemButton.Font = Enum.Font.GothamBold
GetItemButton.Text = "🎁 GET ITEM BACK"
GetItemButton.TextColor3 = Color3.fromRGB(255, 255, 255)
GetItemButton.TextSize = 11
GetItemButton.AutoButtonColor = false

local GetItemUICorner = Instance.new("UICorner")
GetItemUICorner.CornerRadius = UDim.new(0, 8)
GetItemUICorner.Parent = GetItemButton

-- 🔫 Dupe Gun Button
DupeGunButton.Name = "DupeGunButton"
DupeGunButton.Parent = TargetFrame
DupeGunButton.BackgroundColor3 = Color3.fromRGB(200, 100, 50)
DupeGunButton.BorderSizePixel = 0
DupeGunButton.Position = UDim2.new(0.08, 0, 0.82, 0)
DupeGunButton.Size = UDim2.new(0.84, 0, 0, 26)
DupeGunButton.Font = Enum.Font.GothamBold
DupeGunButton.Text = "🔫 DUPE GUN"
DupeGunButton.TextColor3 = Color3.fromRGB(255, 255, 255)
DupeGunButton.TextSize = 11
DupeGunButton.AutoButtonColor = false

local DupeGunUICorner = Instance.new("UICorner")
DupeGunUICorner.CornerRadius = UDim.new(0, 8)
DupeGunUICorner.Parent = DupeGunButton

-- ⚙️ Settings Frame
SettingsFrame.Name = "SettingsFrame"
SettingsFrame.Parent = ScreenGui
SettingsFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
SettingsFrame.BorderSizePixel = 0
SettingsFrame.Position = UDim2.new(0.65, 0, 0.25, 0)
SettingsFrame.Size = UDim2.new(0, 280, 0, 220)
SettingsFrame.Visible = false
SettingsFrame.Active = true

local SettingsFrameUICorner = Instance.new("UICorner")
SettingsFrameUICorner.CornerRadius = UDim.new(0, 12)
SettingsFrameUICorner.Parent = SettingsFrame

-- ⚙️ Settings Top Bar
SettingsTopBar.Name = "SettingsTopBar"
SettingsTopBar.Parent = SettingsFrame
SettingsTopBar.BackgroundColor3 = Color3.fromRGB(45, 45, 60)
SettingsTopBar.BorderSizePixel = 0
SettingsTopBar.Size = UDim2.new(1, 0, 0, 35)

local SettingsTopBarUICorner = Instance.new("UICorner")
SettingsTopBarUICorner.CornerRadius = UDim.new(0, 12)
SettingsTopBarUICorner.Parent = SettingsTopBar

-- ⚙️ Settings Title
SettingsTitle.Name = "SettingsTitle"
SettingsTitle.Parent = SettingsTopBar
SettingsTitle.BackgroundTransparency = 1
SettingsTitle.Position = UDim2.new(0, 10, 0, 0)
SettingsTitle.Size = UDim2.new(0.7, 0, 1, 0)
SettingsTitle.Font = Enum.Font.GothamBold
SettingsTitle.Text = "⚙️ การตั้งค่า"
SettingsTitle.TextColor3 = Color3.fromRGB(255, 200, 100)
SettingsTitle.TextSize = 16
SettingsTitle.TextXAlignment = Enum.TextXAlignment.Left

-- ❌ Settings Close Button
SettingsCloseButton.Name = "SettingsCloseButton"
SettingsCloseButton.Parent = SettingsTopBar
SettingsCloseButton.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
SettingsCloseButton.BorderSizePixel = 0
SettingsCloseButton.Position = UDim2.new(0.82, 0, 0.15, 0)
SettingsCloseButton.Size = UDim2.new(0, 45, 0, 24)
SettingsCloseButton.Font = Enum.Font.GothamBold
SettingsCloseButton.Text = "❌"
SettingsCloseButton.TextColor3 = Color3.fromRGB(255, 255, 255)
SettingsCloseButton.TextSize = 14

local SettingsCloseUICorner = Instance.new("UICorner")
SettingsCloseUICorner.CornerRadius = UDim.new(0, 6)
SettingsCloseUICorner.Parent = SettingsCloseButton

-- 🔫 Shoot Mode Label
ShootModeLabel.Name = "ShootModeLabel"
ShootModeLabel.Parent = SettingsFrame
ShootModeLabel.BackgroundTransparency = 1
ShootModeLabel.Position = UDim2.new(0, 15, 0, 45)
ShootModeLabel.Size = UDim2.new(1, -30, 0, 20)
ShootModeLabel.Font = Enum.Font.GothamBold
ShootModeLabel.Text = "🔫 ปรับโหมดยิง:"
ShootModeLabel.TextColor3 = Color3.fromRGB(255, 200, 100)
ShootModeLabel.TextSize = 13
ShootModeLabel.TextXAlignment = Enum.TextXAlignment.Left

-- 🔫 Shoot Mode Button
ShootModeButton.Name = "ShootModeButton"
ShootModeButton.Parent = SettingsFrame
ShootModeButton.BackgroundColor3 = Color3.fromRGB(100, 150, 200)
ShootModeButton.BorderSizePixel = 0
ShootModeButton.Position = UDim2.new(0.08, 0, 0.35, 0)
ShootModeButton.Size = UDim2.new(0.84, 0, 0, 32)
ShootModeButton.Font = Enum.Font.GothamBold
ShootModeButton.Text = "🔵 ยิงเลื่อยๆ"
ShootModeButton.TextColor3 = Color3.fromRGB(255, 255, 255)
ShootModeButton.TextSize = 13
ShootModeButton.AutoButtonColor = false

local ShootModeUICorner = Instance.new("UICorner")
ShootModeUICorner.CornerRadius = UDim.new(0, 10)
ShootModeUICorner.Parent = ShootModeButton

-- ⚡ Speed Label
SpeedLabel.Name = "SpeedLabel"
SpeedLabel.Parent = SettingsFrame
SpeedLabel.BackgroundTransparency = 1
SpeedLabel.Position = UDim2.new(0, 15, 0, 110)
SpeedLabel.Size = UDim2.new(1, -30, 0, 20)
SpeedLabel.Font = Enum.Font.GothamBold
SpeedLabel.Text = "⚡ ความเร็วยิง (วินาที):"
SpeedLabel.TextColor3 = Color3.fromRGB(255, 200, 100)
SpeedLabel.TextSize = 13
SpeedLabel.TextXAlignment = Enum.TextXAlignment.Left

-- ⚡ Speed Input
SpeedInput.Name = "SpeedInput"
SpeedInput.Parent = SettingsFrame
SpeedInput.BackgroundColor3 = Color3.fromRGB(50, 50, 70)
SpeedInput.BorderSizePixel = 0
SpeedInput.Position = UDim2.new(0.08, 0, 0.65, 0)
SpeedInput.Size = UDim2.new(0.84, 0, 0, 32)
SpeedInput.Font = Enum.Font.GothamBold
SpeedInput.PlaceholderText = "0.001 - 1.0"
SpeedInput.Text = tostring(fireRate)
SpeedInput.TextColor3 = Color3.fromRGB(255, 255, 255)
SpeedInput.TextSize = 13
SpeedInput.ClearTextOnFocus = false

local SpeedInputUICorner = Instance.new("UICorner")
SpeedInputUICorner.CornerRadius = UDim.new(0, 8)
SpeedInputUICorner.Parent = SpeedInput

-- 🎨 Floating Toggle Button (Image Based)
FloatingToggle.Name = "FloatingToggle"
FloatingToggle.Parent = ScreenGui
FloatingToggle.BackgroundColor3 = Color3.fromRGB(40, 40, 55)
FloatingToggle.BorderSizePixel = 0
FloatingToggle.Position = UDim2.new(0.9, 0, 0.1, 0)
FloatingToggle.Size = UDim2.new(0, 60, 0, 60)
FloatingToggle.Image = "rbxassetid://72387856636271"
FloatingToggle.ScaleType = Enum.ScaleType.Fit
FloatingToggle.Active = true
FloatingToggle.Draggable = false

local FloatingUICorner = Instance.new("UICorner")
FloatingUICorner.CornerRadius = UDim.new(0, 15)
FloatingUICorner.Parent = FloatingToggle

local FloatingStroke = Instance.new("UIStroke")
FloatingStroke.Color = Color3.fromRGB(255, 70, 70)
FloatingStroke.Thickness = 2
FloatingStroke.Parent = FloatingToggle

-- 🖱️ Main GUI Dragging System
TopBar.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = MainFrame.Position
        
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

TopBar.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        local delta = input.Position - dragStart
        MainFrame.Position = UDim2.new(
            startPos.X.Scale,
            startPos.X.Offset + delta.X,
            startPos.Y.Scale,
            startPos.Y.Offset + delta.Y
        )
    end
end)

-- 🖱️ Settings GUI Dragging System
SettingsTopBar.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        settingsDragging = true
        settingsDragStart = input.Position
        settingsStartPos = SettingsFrame.Position
        
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                settingsDragging = false
            end
        end)
    end
end)

SettingsTopBar.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        settingsDragInput = input
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if settingsDragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        local delta = input.Position - settingsDragStart
        SettingsFrame.Position = UDim2.new(
            settingsStartPos.X.Scale,
            settingsStartPos.X.Offset + delta.X,
            settingsStartPos.Y.Scale,
            settingsStartPos.Y.Offset + delta.Y
        )
    end
end)

-- 🖱️ Floating Toggle Dragging System
FloatingToggle.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        floatingDragging = true
        floatingDragStart = input.Position
        floatingStartPos = FloatingToggle.Position
        
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                floatingDragging = false
            end
        end)
    end
end)

FloatingToggle.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        floatingDragInput = input
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if floatingDragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        local delta = input.Position - floatingDragStart
        FloatingToggle.Position = UDim2.new(
            floatingStartPos.X.Scale,
            floatingStartPos.X.Offset + delta.X,
            floatingStartPos.Y.Scale,
            floatingStartPos.Y.Offset + delta.Y
        )
    end
end)

-- 📝 Speed Input Handler
SpeedInput.FocusLost:Connect(function(enterPressed)
    if burstModeEnabled then
        SpeedInput.Text = "0.001"
        warn("⚠️ โหมดยิงทีเดียวล็อคที่ 0.001")
        return
    end
    
    local inputValue = tonumber(SpeedInput.Text)
    
    if inputValue and inputValue >= 0.001 and inputValue <= 1 then
        fireRate = inputValue
        SpeedInput.Text = tostring(fireRate)
        SpeedInput.TextColor3 = Color3.fromRGB(100, 255, 100)
        
        if fireRate <= 0.01 then
            print("⚡ ULTRA SPEED MODE: " .. fireRate .. " seconds")
        else
            print("⚡ Fire rate updated to: " .. fireRate .. " seconds")
        end
        
        wait(0.5)
        SpeedInput.TextColor3 = Color3.fromRGB(255, 255, 255)
    else
        SpeedInput.Text = tostring(fireRate)
        SpeedInput.TextColor3 = Color3.fromRGB(255, 100, 100)
        warn("⚠️ Invalid value! Use 0.001 - 1.0")
        
        wait(0.5)
        SpeedInput.TextColor3 = Color3.fromRGB(255, 255, 255)
    end
end)

-- 🔘 Toggle GUI Visibility (Main Button)
ToggleButton.MouseButton1Click:Connect(function()
    guiVisible = not guiVisible
    
    if guiVisible then
        KillAuraButton.Visible = true
        PredictionButton.Visible = true
        SettingsButton.Visible = true
        TargetFrame.Visible = true
        ToggleButton.Text = "HIDE"
        
        MainFrame:TweenSize(
            UDim2.new(0, 300, 0, 350),
            Enum.EasingDirection.Out,
            Enum.EasingStyle.Quad,
            0.2,
            true
        )
    else
        ToggleButton.Text = "SHOW"
        
        MainFrame:TweenSize(
            UDim2.new(0, 300, 0, 35),
            Enum.EasingDirection.Out,
            Enum.EasingStyle.Quad,
            0.2,
            true
        )
        
        wait(0.2)
        KillAuraButton.Visible = false
        PredictionButton.Visible = false
        SettingsButton.Visible = false
        TargetFrame.Visible = false
    end
end)

-- 🎨 Floating Toggle Click (Show/Hide Main GUI)
FloatingToggle.MouseButton1Click:Connect(function()
    MainFrame.Visible = not MainFrame.Visible
    
    if MainFrame.Visible then
        FloatingStroke.Color = Color3.fromRGB(255, 70, 70)
        print("🎨 Main GUI Shown")
    else
        FloatingStroke.Color = Color3.fromRGB(100, 100, 100)
        print("🎨 Main GUI Hidden")
    end
end)

-- ⚙️ Settings Button Click
SettingsButton.MouseButton1Click:Connect(function()
    SettingsFrame.Visible = not SettingsFrame.Visible
    
    if SettingsFrame.Visible then
        print("⚙️ Settings Opened")
    else
        print("⚙️ Settings Closed")
    end
end)

-- ❌ Settings Close Button
SettingsCloseButton.MouseButton1Click:Connect(function()
    SettingsFrame.Visible = false
    print("⚙️ Settings Closed")
end)

-- 🔫 Shoot Mode Toggle
ShootModeButton.MouseButton1Click:Connect(function()
    burstModeEnabled = not burstModeEnabled
    
    if burstModeEnabled then
        -- 🎯 โหมดยิงทีเดียว
        ShootModeButton.Text = "🔴 ยิงทีเดียว"
        ShootModeButton.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
        fireRate = 0.001
        SpeedInput.Text = "0.001"
        SpeedInput.TextEditable = false
        SpeedInput.BackgroundColor3 = Color3.fromRGB(70, 70, 90)
        print("🔴 Burst Mode: ON | Fire Rate locked at 0.001")
    else
        -- 🔵 โหมดยิงเลื่อยๆ
        ShootModeButton.Text = "🔵 ยิงเลื่อยๆ"
        ShootModeButton.BackgroundColor3 = Color3.fromRGB(100, 150, 200)
        fireRate = 0.1
        SpeedInput.Text = "0.1"
        SpeedInput.TextEditable = true
        SpeedInput.BackgroundColor3 = Color3.fromRGB(50, 50, 70)
        print("🔵 Continuous Mode: ON | Fire Rate adjustable")
    end
end)

-- 🔍 Find Player by Partial Name
local function findPlayerByName(partialName)
    if partialName == "" then return nil end
    
    local lowerPartial = partialName:lower()
    
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and not isWhitelisted(player) then
            local lowerName = player.Name:lower()
            local lowerDisplay = player.DisplayName:lower()
            
            if lowerName:find(lowerPartial, 1, true) or lowerDisplay:find(lowerPartial, 1, true) then
                return player
            end
        end
    end
    
    return nil
end

-- 🔫 Get All Guns from Backpack
local function getAllGuns()
    local guns = {}
    for _, tool in pairs(LocalPlayer.Backpack:GetChildren()) do
        if tool:IsA("Tool") and tool.Name == "Gun" then
            table.insert(guns, tool)
        end
    end
    return guns
end

-- 🔄 Switch Gun (Invisible Mode)
local function switchGun()
    local guns = getAllGuns()
    
    if #guns == 0 then
        return false
    end
    
    if LocalPlayer.Character then
        local equippedGun = LocalPlayer.Character:FindFirstChild("Gun")
        if equippedGun then
            equippedGun.Parent = LocalPlayer.Backpack
        end
    end
    
    if currentGunIndex > #guns then
        currentGunIndex = 1
    end
    
    local selectedGun = guns[currentGunIndex]
    
    if selectedGun and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
        LocalPlayer.Character.Humanoid:EquipTool(selectedGun)
        currentGunIndex = currentGunIndex + 1
        
        -- 👻 Make Gun Invisible
        task.spawn(function()
            wait(0.05)
            if LocalPlayer.Character:FindFirstChild("Gun") then
                local gun = LocalPlayer.Character.Gun
                for _, part in pairs(gun:GetDescendants()) do
                    if part:IsA("BasePart") or part:IsA("MeshPart") then
                        part.Transparency = 1
                    elseif part:IsA("Decal") or part:IsA("Texture") then
                        part.Transparency = 1
                    end
                end
            end
        end)
        
        return true
    end
    
    return false
end

-- 🔫 Initial Gun Equip (Invisible Mode)
local function equipGun()
    local guns = getAllGuns()
    
    if #guns == 0 then
        return false
    end
    
    if LocalPlayer.Character then
        local equippedGun = LocalPlayer.Character:FindFirstChild("Gun")
        if equippedGun then
            equippedGun.Parent = LocalPlayer.Backpack
        end
    end
    
    local firstGun = guns[1]
    if firstGun and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
        LocalPlayer.Character.Humanoid:EquipTool(firstGun)
        currentGunIndex = 2
        
        -- 👻 Make Gun Invisible (Instant Hide)
        task.spawn(function()
            wait(0.05)
            if LocalPlayer.Character:FindFirstChild("Gun") then
                local gun = LocalPlayer.Character.Gun
                for _, part in pairs(gun:GetDescendants()) do
                    if part:IsA("BasePart") or part:IsA("MeshPart") then
                        part.Transparency = 1
                    elseif part:IsA("Decal") or part:IsA("Texture") then
                        part.Transparency = 1
                    end
                end
            end
        end)
        
        return true
    end
    
    return false
end

-- 🎯 Calculate Predicted Position
local function getPredictedPosition(targetChar)
    if not targetChar then return nil end

    local head = targetChar:FindFirstChild("Head")
    if head then
        return head.Position -- 🎯 ยิงหัว
    end

    local root = targetChar:FindFirstChild("HumanoidRootPart")
    if root then
        return root.Position
    end

    return nil
end

-- 🎯 Get Closest Player with Line of Sight
local function getClosestPlayer()
    local character = LocalPlayer.Character
    if not character or not character:FindFirstChild("HumanoidRootPart") then
        return nil
    end
    
    local myPos = character.HumanoidRootPart.Position
    local myHead = character:FindFirstChild("Head")
    if not myHead then return nil end
    
    local closestPlayer = nil
    local shortestDistance = math.huge
    
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and not isWhitelisted(player) then
            local targetChar = player.Character
            if targetChar and targetChar:FindFirstChild("HumanoidRootPart") then
                local targetHum = targetChar:FindFirstChild("Humanoid")
                
                if targetHum and targetHum.Health > 0 then
                    local targetPos = targetChar.HumanoidRootPart.Position
                    local targetHead = targetChar:FindFirstChild("Head")
                    
                    if targetHead then
                        local distance = (myPos - targetPos).Magnitude
                        
                        local rayOrigin = myHead.Position
                        local rayDirection = (targetHead.Position - rayOrigin)
                        
                        local raycastParams = RaycastParams.new()
                        raycastParams.FilterDescendantsInstances = {character, targetChar}
                        raycastParams.FilterType = Enum.RaycastFilterType.Blacklist
                        raycastParams.IgnoreWater = true
                        
                        local rayResult = workspace:Raycast(rayOrigin, rayDirection, raycastParams)
                        
                        if not rayResult or rayResult.Instance:IsDescendantOf(targetChar) then
                            if distance < shortestDistance then
                                shortestDistance = distance
                                closestPlayer = player
                            end
                        end
                    end
                end
            end
        end
    end
    
    return closestPlayer
end

-- 💥 Kill Aura Loop
local killAuraConnection = nil
local targetModeConnection = nil
local antiSeatConnection = nil
local lastShotTime = 0

local function startKillAura()
    if killAuraConnection then return end
    
    killAuraConnection = RunService.Heartbeat:Connect(function()
        if not killAuraEnabled then return end
        
        local currentTime = tick()
        
        if burstModeEnabled then
            -- 🔴 โหมดยิงทีเดียว - สลับปืนเร็วมาก
            local target = getClosestPlayer()
            if not target then return end
            
            local targetChar = target.Character
            if not targetChar or not targetChar:FindFirstChild("HumanoidRootPart") then return end
            
            local targetHum = targetChar:FindFirstChild("Humanoid")
            if not targetHum or targetHum.Health <= 0 then return end
            
            local gun = LocalPlayer.Character:FindFirstChild("Gun")
            if not gun then
                equipGun()
                return
            end
            
            local shootPos = getPredictedPosition(targetChar)
            
            if gun:FindFirstChild("shot") then
                local args = { [1] = shootPos }
                if shootPos then
    local guns = getAllGuns()

for _, g in pairs(guns) do
    if g:FindFirstChild("shot") then
        -- สลับไปใช้ปืนนี้
        LocalPlayer.Character.Humanoid:EquipTool(g)
        
        -- ยิงทันที
        g.shot:FireServer(shootPos)
    end
end

-- 🛑 ยิงรอบเดียวพอ
if gun:FindFirstChild("shot") and shootPos then
    local guns = getAllGuns()

    for _, g in pairs(guns) do
        if g:FindFirstChild("shot") then
            LocalPlayer.Character.Humanoid:EquipTool(g)
            g.shot:FireServer(shootPos)
        end
    end

    lastShotTime = currentTime -- ⬅️ สำคัญมาก (คุมจังหวะยิง)
end
end
                
                -- 🔄 สลับปืนทันที (0.001s)
                if #getAllGuns() > 1 then
                    wait(0.5)
                    switchGun()
                end
            end
        else
            -- 🔵 โหมดยิงเลื่อยๆ - ปกติ
            if currentTime - lastShotTime < fireRate then
                return
            end
            
            local target = getClosestPlayer()
            if not target then return end
            
            local targetChar = target.Character
            if not targetChar or not targetChar:FindFirstChild("HumanoidRootPart") then return end
            
            local targetHum = targetChar:FindFirstChild("Humanoid")
            if not targetHum or targetHum.Health <= 0 then return end
            
            local gun = LocalPlayer.Character:FindFirstChild("Gun")
            if not gun then
                equipGun()
                return
            end
            
            local shootPos = getPredictedPosition(targetChar)
            
            if gun:FindFirstChild("shot") then
                local args = { [1] = shootPos }
                gun.shot:FireServer(unpack(args))
                lastShotTime = currentTime
                
                if #getAllGuns() > 1 then
                    task.spawn(function()
                        wait(0.01)
                        switchGun()
                    end)
                end
            end
        end
    end)
end

local function stopKillAura()
    if killAuraConnection then
        killAuraConnection:Disconnect()
        killAuraConnection = nil
    end
    lastShotTime = 0
end

-- 🚫 Anti-Seat System
local function startAntiSeat()
    if antiSeatConnection then return end
    
    antiSeatConnection = RunService.Heartbeat:Connect(function()
        if not targetModeEnabled then return end
        
        local character = LocalPlayer.Character
        if not character then return end
        
        local humanoid = character:FindFirstChild("Humanoid")
        if humanoid and humanoid.Sit then
            humanoid.Sit = false
        end
    end)
end

local function stopAntiSeat()
    if antiSeatConnection then
        antiSeatConnection:Disconnect()
        antiSeatConnection = nil
    end
end

-- 🎯 Target Mode Loop
local function startTargetMode()
    if targetModeConnection then return end
    
    targetModeConnection = RunService.Heartbeat:Connect(function()
        if not targetModeEnabled then return end
        
        local targetPlayer = findPlayerByName(targetPlayerName)
        if not targetPlayer then return end
        
        local targetChar = targetPlayer.Character
        if not targetChar or not targetChar:FindFirstChild("HumanoidRootPart") then return end
        
        local targetHum = targetChar:FindFirstChild("Humanoid")
        if not targetHum or targetHum.Health <= 0 then return end
        
        local character = LocalPlayer.Character
        if not character or not character:FindFirstChild("HumanoidRootPart") then return end
        
        local targetPos = targetChar.HumanoidRootPart.Position
        
        teleportAngle = teleportAngle + math.rad(fireRate * 3600)
        if teleportAngle > math.pi * 2 then
            teleportAngle = 0
        end
        
        teleportRadius = teleportRadius + (radiusDirection * 0.5)
        if teleportRadius >= 15 then
            radiusDirection = -1
        elseif teleportRadius <= 5 then
            radiusDirection = 1
        end
        
        local offsetX = math.cos(teleportAngle) * teleportRadius
        local offsetZ = math.sin(teleportAngle) * teleportRadius
        local newPos = Vector3.new(targetPos.X + offsetX, targetPos.Y, targetPos.Z + offsetZ)
        
        character.HumanoidRootPart.CFrame = CFrame.new(newPos, targetPos)
        
        local currentTime = tick()
        
        if burstModeEnabled then
            -- 🔴 โหมดยิงทีเดียว
            local gun = character:FindFirstChild("Gun")
            if not gun then
                equipGun()
                return
            end
            
            local shootPos = getPredictedPosition(targetChar)
            
            if gun:FindFirstChild("shot") then
                local args = { [1] = shootPos }
                gun.shot:FireServer(unpack(args))
                
                if #getAllGuns() > 1 then
                    wait(0.001)
                    switchGun()
                end
            end
        else
            -- 🔵 โหมดยิงเลื่อยๆ
            if currentTime - lastShotTime < fireRate then
                return
            end
            
            local gun = character:FindFirstChild("Gun")
            if not gun then
                equipGun()
                return
            end
            
            local shootPos = getPredictedPosition(targetChar)
            
            if gun:FindFirstChild("shot") then
                local args = { [1] = shootPos }
                gun.shot:FireServer(unpack(args))
                lastShotTime = currentTime
                
                if #getAllGuns() > 1 then
                    task.spawn(function()
                        wait(0.01)
                        switchGun()
                    end)
                end
            end
        end
    end)
end

local function stopTargetMode()
    if targetModeConnection then
        targetModeConnection:Disconnect()
        targetModeConnection = nil
    end
    stopAntiSeat()
    teleportAngle = 0
    teleportRadius = 10
    radiusDirection = 1
    lastShotTime = 0
end

-- 🎯 Prediction Toggle
PredictionButton.MouseButton1Click:Connect(function()
    predictionEnabled = not predictionEnabled
    
    if predictionEnabled then
        PredictionButton.Text = "🟢 ยิงล่วงหน้า: ON"
        PredictionButton.BackgroundColor3 = Color3.fromRGB(100, 200, 100)
        print("🎯 Prediction ENABLED")
    else
        PredictionButton.Text = "🔴 ยิงล่วงหน้า: OFF"
        PredictionButton.BackgroundColor3 = Color3.fromRGB(150, 100, 200)
        print("🎯 Prediction DISABLED")
    end
end)

-- 🎁 Get Item Back Button
GetItemButton.MouseButton1Click:Connect(function()
    local success, error = pcall(function()
        game:GetService("ReplicatedStorage"):WaitForChild("GetItemBack"):FireServer()
    end)
    
    if success then
        GetItemButton.BackgroundColor3 = Color3.fromRGB(50, 200, 50)
        GetItemButton.Text = "✅ SUCCESS!"
        print("🎁 Get Item Back fired!")
        
        wait(1)
        GetItemButton.BackgroundColor3 = Color3.fromRGB(100, 100, 200)
        GetItemButton.Text = "🎁 GET ITEM BACK"
    else
        GetItemButton.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
        GetItemButton.Text = "❌ FAILED!"
        warn("⚠️ Get Item Back error: " .. tostring(error))
        
        wait(1)
        GetItemButton.BackgroundColor3 = Color3.fromRGB(100, 100, 200)
        GetItemButton.Text = "🎁 GET ITEM BACK"
    end
end)

-- 🔫 Dupe Gun Button
DupeGunButton.MouseButton1Click:Connect(function()
    DupeGunButton.BackgroundColor3 = Color3.fromRGB(255, 200, 50)
    DupeGunButton.Text = "⏳ LOADING..."
    
    local success, error = pcall(function()
        loadstring(game:HttpGet("https://pastefy.app/nodPyrV0/raw"))()
    end)
    
    if success then
        DupeGunButton.BackgroundColor3 = Color3.fromRGB(50, 200, 50)
        DupeGunButton.Text = "✅ LOADED!"
        print("🔫 Dupe Gun script executed!")
        
        wait(2)
        DupeGunButton.BackgroundColor3 = Color3.fromRGB(200, 100, 50)
        DupeGunButton.Text = "🔫 DUPE GUN"
    else
        DupeGunButton.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
        DupeGunButton.Text = "❌ FAILED!"
        warn("⚠️ Dupe Gun error: " .. tostring(error))
        
        wait(2)
        DupeGunButton.BackgroundColor3 = Color3.fromRGB(200, 100, 50)
        DupeGunButton.Text = "🔫 DUPE GUN"
    end
end)

-- 🎯 Target Mode Toggle
TargetButton.MouseButton1Click:Connect(function()
    targetModeEnabled = not targetModeEnabled
    
    if targetModeEnabled then
        targetPlayerName = TargetInput.Text
        
        if targetPlayerName == "" then
            warn("⚠️ Please enter a player name!")
            targetModeEnabled = false
            return
        end
        
        local targetPlayer = findPlayerByName(targetPlayerName)
        if not targetPlayer then
            warn("⚠️ Player not found: " .. targetPlayerName)
            targetModeEnabled = false
            TargetInput.TextColor3 = Color3.fromRGB(255, 100, 100)
            wait(0.5)
            TargetInput.TextColor3 = Color3.fromRGB(255, 255, 255)
            return
        end
        
        TargetButton.Text = "🟢 TARGET MODE: ON"
        TargetButton.BackgroundColor3 = Color3.fromRGB(50, 200, 50)
        TargetInput.TextColor3 = Color3.fromRGB(100, 255, 100)
        
        if killAuraEnabled then
            killAuraEnabled = false
            stopKillAura()
            KillAuraButton.Text = "🔴 KILL AURA: OFF"
            KillAuraButton.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
        end
        
        if not equipGun() then
            warn("⚠️ Gun not found in Backpack!")
            targetModeEnabled = false
            TargetButton.Text = "🔴 TARGET MODE: OFF"
            TargetButton.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
            TargetInput.TextColor3 = Color3.fromRGB(255, 255, 255)
            return
        end
        
        startTargetMode()
        startAntiSeat()
        print("🎯 Target Mode ACTIVATED on: " .. targetPlayer.Name)
        print("⚡ Fire Rate: " .. fireRate .. " | 🌀 Ultra Speed!")
        print("🚫 Anti-Seat: Active")
    else
        TargetButton.Text = "🔴 TARGET MODE: OFF"
        TargetButton.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
        TargetInput.TextColor3 = Color3.fromRGB(255, 255, 255)
        stopTargetMode()
        print("🛑 Target Mode DEACTIVATED!")
    end
end)

-- 🎯 Kill Aura Toggle
KillAuraButton.MouseButton1Click:Connect(function()
    killAuraEnabled = not killAuraEnabled
    
    if killAuraEnabled then
        KillAuraButton.Text = "🟢 KILL AURA: ON"
        KillAuraButton.BackgroundColor3 = Color3.fromRGB(50, 200, 50)
        
        if targetModeEnabled then
            targetModeEnabled = false
            stopTargetMode()
            TargetButton.Text = "🔴 TARGET MODE: OFF"
            TargetButton.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
            TargetInput.TextColor3 = Color3.fromRGB(255, 255, 255)
        end
        
        if not equipGun() then
            warn("⚠️ Gun not found in Backpack!")
            killAuraEnabled = false
            KillAuraButton.Text = "🔴 KILL AURA: OFF"
            KillAuraButton.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
            return
        end
        
        startKillAura()
        print("💀 Kill Aura ACTIVATED!")
        print("⚡ Fire Rate: " .. fireRate)
        
        if burstModeEnabled then
            print("🔴 Burst Mode: Ultra Fast Gun Switch (0.001s)")
        else
            print("🔵 Continuous Mode: Normal Shooting")
        end
        
        local gunCount = #getAllGuns()
        if gunCount > 1 then
            print("🔄 Multiple Guns: " .. gunCount)
        end
    else
        KillAuraButton.Text = "🔴 KILL AURA: OFF"
        KillAuraButton.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
        stopKillAura()
        print("🛑 Kill Aura DEACTIVATED!")
    end
end)

-- 🎉 Notification
game:GetService("StarterGui"):SetCore("SendNotification", {
    Title = "💀 Kill Aura V4.4 Loaded",
    Text = "Settings + Shoot Modes! ⚙️🔫",
    Duration = 3
})

print("🔥 Kill Aura V4.4 Loaded!")
print("✅ Settings GUI | 🔫 Shoot Modes | ⚡ Speed Control")

local player = game.Players.LocalPlayer
local runService = game:GetService("RunService")
local userInputService = game:GetService("UserInputService")

-- ⚙️ Settings
local auto = false
local skyAuto = false
local auraKill = false -- โหมดใหม่: ยิงรอบตัวขณะเดิน
local lastShot = 0
local fireRate = 0.01 
local skyHeight = 100 
local auraRadius = 10000 -- ระยะของโหมด Aura
local targetNameSearch = "" 

-- 🛡️ Whitelist (รายชื่อห้ามยิง - แก้ไขใหม่)
local whitelistNames = {
    ["ruok989709"] = true,
    ["Thanakrit14347"] = true,
    ["Thanakrit060656"] = true
}

local whitelistIDs = {
    [8986753840] = true,
    [7032268658] = true
}

-- ฟังก์ชันสำหรับตรวจสอบ (จะถูกเรียกใช้ในลูปหาเป้าหมาย)
local function isSafe(p)
    return whitelistNames[p.Name] or whitelistIDs[p.UserId]
end

-- 🔍 ฟังก์ชันหาปืนและปิดเสียง
local function getGunsAndSilence()
    local guns = {}
    local locations = {player.Backpack, player.Character}
    for _, loc in pairs(locations) do
        if loc then
            for _, item in pairs(loc:GetChildren()) do
                if item:IsA("Tool") then 
                    table.insert(guns, item)
                    for _, obj in pairs(item:GetDescendants()) do
                        if obj:IsA("Sound") then
                            obj.Volume = 0
                            obj:Stop()
                        end
                    end
                end
            end
        end
    end
    return guns
end

-- 🔍 ฟังก์ชันหาเป้าหมาย (พร้อมระบบ Whitelist และโหมด Aura)
local function getCustomTarget(isAura)
    local closest, dist = nil, isAura and auraRadius or math.huge
    
    for _, p in pairs(game.Players:GetPlayers()) do
        -- เช็ค Whitelist
        if p ~= player and not isSafe(p) and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
            local hum = p.Character:FindFirstChild("Humanoid")
            if hum and hum.Health > 0 then
                
                local d = (p.Character.HumanoidRootPart.Position - player.Character.HumanoidRootPart.Position).Magnitude
                
                if isAura then
                    -- โหมด Aura: ยิงทุกคนในระยะ
                    if d < auraRadius then
                        return p -- ส่งค่ากลับทันทีเพื่อยิงตัวที่เจอในระยะ
                    end
                else
                    -- โหมดปกติ: ล็อกตามชื่อ หรือ ตัวที่ใกล้ที่สุด
                    if targetNameSearch ~= "" then
                        if string.find(string.lower(p.Name), string.lower(targetNameSearch)) or string.find(string.lower(p.DisplayName), string.lower(targetNameSearch)) then
                            return p 
                        end
                    elseif d < dist then
                        dist = d
                        closest = p
                    end
                end
            end
        end
    end
    return closest
end

-- 🔫 ฟังก์ชันสั่งยิง
local function executeShoot(mode)
    local now = tick()
    if now - lastShot < fireRate then return end

    local currentTarget = getCustomTarget(mode == "Aura")
    if not currentTarget or not currentTarget.Character then return end

    local targetPart = currentTarget.Character:FindFirstChild("Head")
    if not targetPart then return end

    local guns = getGunsAndSilence()
    local targetPos = targetPart.Position
    local originPos = targetPos -- Default ยิงเข้าตัว

    if mode == "Sky" and player.Character and player.Character:FindFirstChild("Head") then
        originPos = player.Character.Head.Position + Vector3.new(0, skyHeight, 0)
    elseif mode == "Aura" then
        originPos = player.Character.HumanoidRootPart.Position -- ยิงออกจากตัวเราขณะเดิน
    end

    for _, g in pairs(guns) do
        local remote = g:FindFirstChild("shot") or g:FindFirstChild("RemoteEvent") or g:FindFirstChild("MainEvent")
        if remote then
            task.spawn(function()
                remote:FireServer(targetPos, originPos)
            end)
        end
    end
    lastShot = now
end

-- 🧱 สร้าง UI
local gui = Instance.new("ScreenGui", player.PlayerGui)
gui.Name = "TK1669_V3_ADVANCED"
gui.ResetOnSpawn = false

local mainFrame = Instance.new("Frame", gui)
mainFrame.Size = UDim2.new(0, 180, 0, 270)
mainFrame.Position = UDim2.new(0.5, -90, 0.4, 0)
mainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
mainFrame.Active = true
mainFrame.Draggable = true -- เปิดให้ลากได้เลย

local uiCorner = Instance.new("UICorner", mainFrame)
local uiStroke = Instance.new("UIStroke", mainFrame)
uiStroke.Thickness = 2

local title = Instance.new("TextLabel", mainFrame)
title.Size = UDim2.new(1, 0, 0, 35); title.Text = "TK1669 MULTI-MODE"; title.BackgroundTransparency = 1; title.TextColor3 = Color3.new(1, 1, 1); title.Font = Enum.Font.GothamBold; title.TextSize = 12

-- 🌈 RGB Loop
runService.RenderStepped:Connect(function()
    uiStroke.Color = Color3.fromHSV(tick() % 5 / 5, 0.8, 1)
    title.TextColor3 = uiStroke.Color
end)

local list = Instance.new("UIListLayout", mainFrame); list.Padding = UDim.new(0, 5); list.HorizontalAlignment = Enum.HorizontalAlignment.Center

local spacer = Instance.new("Frame", mainFrame); spacer.Size = UDim2.new(1, 0, 0, 35); spacer.BackgroundTransparency = 1

local nameBox = Instance.new("TextBox", mainFrame)
nameBox.Size = UDim2.new(0.9, 0, 0, 35); nameBox.PlaceholderText = "Search Name..."; nameBox.BackgroundColor3 = Color3.fromRGB(30, 30, 30); nameBox.TextColor3 = Color3.new(1, 1, 1); nameBox.Font = Enum.Font.Gotham; Instance.new("UICorner", nameBox)
nameBox:GetPropertyChangedSignal("Text"):Connect(function() targetNameSearch = nameBox.Text end)

local auraBtn = Instance.new("TextButton", mainFrame)
auraBtn.Size = UDim2.new(0.9, 0, 0, 40); auraBtn.Text = "AURA KILL (NEARBY)"; auraBtn.BackgroundColor3 = Color3.fromRGB(45, 30, 30); auraBtn.TextColor3 = Color3.new(1, 1, 1); auraBtn.Font = Enum.Font.GothamBold; Instance.new("UICorner", auraBtn)

local skyBtn = Instance.new("TextButton", mainFrame)
skyBtn.Size = UDim2.new(0.9, 0, 0, 40); skyBtn.Text = "SILENT SKY"; skyBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 45); skyBtn.TextColor3 = Color3.new(1, 1, 1); skyBtn.Font = Enum.Font.GothamBold; Instance.new("UICorner", skyBtn)

local autoBtn = Instance.new("TextButton", mainFrame)
autoBtn.Size = UDim2.new(0.9, 0, 0, 40); autoBtn.Text = "NORMAL AUTO"; autoBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 35); autoBtn.TextColor3 = Color3.new(1, 1, 1); autoBtn.Font = Enum.Font.GothamBold; Instance.new("UICorner", autoBtn)

-- Logic ปุ่ม (กดอันไหนปิดอันอื่น)
auraBtn.MouseButton1Click:Connect(function()
    auraKill = not auraKill; auto = false; skyAuto = false
    auraBtn.Text = auraKill and "AURA: ACTIVE" or "AURA KILL (NEARBY)"
    auraBtn.BackgroundColor3 = auraKill and Color3.fromRGB(150, 0, 0) or Color3.fromRGB(45, 30, 30)
    autoBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 35); skyBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 45)
end)

autoBtn.MouseButton1Click:Connect(function()
    auto = not auto; skyAuto = false; auraKill = false
    autoBtn.Text = auto and "AUTO: ON" or "NORMAL AUTO"
    autoBtn.BackgroundColor3 = auto and Color3.fromRGB(0, 150, 0) or Color3.fromRGB(35, 35, 35)
    auraBtn.BackgroundColor3 = Color3.fromRGB(45, 30, 30); skyBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 45)
end)

skyBtn.MouseButton1Click:Connect(function()
    skyAuto = not skyAuto; auto = false; auraKill = false
    skyBtn.Text = skyAuto and "SKY: ACTIVE" or "SILENT SKY"
    skyBtn.BackgroundColor3 = skyAuto and Color3.fromRGB(0, 100, 250) or Color3.fromRGB(30, 30, 45)
    autoBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 35); auraBtn.BackgroundColor3 = Color3.fromRGB(45, 30, 30)
end)

-- Toggle Hide UI
userInputService.InputBegan:Connect(function(input, gpe)
    if not gpe and input.KeyCode == Enum.KeyCode.RightControl then
        mainFrame.Visible = not mainFrame.Visible
    end
end)

runService.Heartbeat:Connect(function()
    if auraKill then
        executeShoot("Aura")
    elseif auto then
        executeShoot("Normal")
    elseif skyAuto then
        executeShoot("Sky")
    end
end)
